<Window x:Class="ZoDream.Player.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:control="clr-namespace:ZoDream.Player.Controls"
        mc:Ignorable="d"
        Background="Transparent"
        AllowsTransparency="True"
        Title="MainWindow" Height="600" Width="300" WindowStyle="None">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="200"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Background="#fff">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <control:SpectrumPanel/>
            <control:FullPlayerPanel Grid.Row="1"/>
        </Grid>
        <Grid  Background="#fff" Visibility="Collapsed">
            <control:LyricsPanel/>
            <control:PlayerPanel/>
        </Grid>
        <control:CatalogBox Grid.Row="1"/>
    </Grid>
</Window>
